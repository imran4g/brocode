

assert = chai.assert;

describe('Testing function generateNum() of Task 1', function () {

    // for easy mode first
    var result = guessNumber(false);

    it('Test 1: generateNum() returns something', function () {
        assert.exists(result, 'the return value is not null or undefined');
      });

});

  